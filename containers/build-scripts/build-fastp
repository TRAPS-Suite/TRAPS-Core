# Build FASTP Container
#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DEF_FILE="${SCRIPT_DIR}/../fastp.def"
SIF_FILE="${SCRIPT_DIR}/../fastp.sif"

apptainer build $SIF_FILE $DEF_FILE 
