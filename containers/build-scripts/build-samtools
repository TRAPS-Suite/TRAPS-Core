# Build samtools Container
#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DEF_FILE="${SCRIPT_DIR}/../samtools.def"
SIF_FILE="${SCRIPT_DIR}/../samtools.sif"

apptainer build $SIF_FILE $DEF_FILE 
