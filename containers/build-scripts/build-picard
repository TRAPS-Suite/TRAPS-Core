# Build Picard Container
#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DEF_FILE="${SCRIPT_DIR}/../picard.def"
SIF_FILE="${SCRIPT_DIR}/../picard.sif"

apptainer build $SIF_FILE $DEF_FILE 
