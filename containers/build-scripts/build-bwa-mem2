# Build BWA-MEM2 Container
#!/bin/bash
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
DEF_FILE="${SCRIPT_DIR}/../bwa-mem2.def"
SIF_FILE="${SCRIPT_DIR}/../bwa-mem2.sif"

apptainer build $SIF_FILE $DEF_FILE 
